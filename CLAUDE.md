# 식품 쇼츠 소재 검증 프로젝트

이 저장소는 유튜브 식품 쇼츠 채널의 **소재 검증을 자동화**하기 위한 작업 공간입니다.

## 작업 흐름 (중요)

1. 운영자가 `소재메모.md`의 "검증 대기"에 `- [ ] 제품명 — 메모` 형식으로 새 소재를 추가한다.
2. Claude Code에서 `/validate` 를 실행한다.
3. `/validate` 루틴이 정해진 단계대로:
   - 대기 소재를 위에서부터 **최대 20건** 가져와
   - 각 소재를 **웹검색으로 사실 확인**한 뒤 적합성 검증 + **대본 초안**까지 작성하고
   - **적합 항목만** `평가결과.md`에 **이어붙이기(누적)** 로 기록하고 (아래 규칙 참조)
   - `소재메모.md`의 해당 항목에 **검증 완료 표시**를 한 뒤
   - **`python3 tools/적합추출.py`를 실행해 `촬영대기.md`를 갱신하고** (아래 참조)
   - 변경사항을 커밋한 뒤 **매 회차 반드시 `main` 브랜치까지 올린다.**

### 판정별 기록 위치 (2026-08-06 운영자 확정 — 매번 확인 없이 그대로 실행할 것)

| 판정 | 기록 위치 |
|------|-----------|
| **적합** | `평가결과.md`에 전체 내용(검증 요약·점수·헤드라인·대본·[확인 필요]·운영자 조치) |
| **보류 · 부적합** | `소재메모.md`에 **판정 사실만** (`- [x] 제품명 ✅ 날짜 · 보류`). 상세 판정 내용은 `보류부적합_보관.md`에 이어붙인다 |

- **`평가결과.md`에는 적합 외의 판정을 절대 넣지 않는다.** 섞이면 `tools/적합추출.py`가 경고를 띄운다.
- **중복 검증 여부는 `소재메모.md`로 판단한다.** 같은 제품이 이미 있으면 `- [x]` 표시와 판정이 붙어 있다.
  검증 전 `소재메모.md`에서 제품명·브랜드를 먼저 검색할 것.
- **축(반전 포인트) 소진 여부는 `평가결과.md`에서 확인한다.** 적합으로 대본이 나온 브랜드는 그 축이 소진된 것이다.
- 보류였던 항목을 다시 검증해 적합이 되면, 그때 `평가결과.md`로 옮겨 적는다(재검증 형식은 아래 참조).

### 촬영대기.md 갱신 규칙 (매 회차 필수)

- 회차가 끝나면 **반드시** 아래를 실행한다. 새로 적합이 나오지 않은 회차에도 실행한다(집계 수치가 바뀐다).
  ```
  python3 tools/적합추출.py
  ```
- 이 스크립트는 `평가결과.md` 전체를 다시 읽어 **적합 목록**을 `촬영대기.md`로 **통째로 다시 만든다.**
  따로 "추가"하는 작업이 필요 없고, 회차마다 실행하면 새 적합이 자동으로 들어온다.
- `촬영대기.md`에 적는 항목은 **제품명 · 적합여부 · 검증날짜 세 개뿐**이다.
  상세 정보와 대본은 `평가결과.md`에 있으므로 중복해서 넣지 않는다.
- **`촬영대기.md`는 손으로 수정하지 않는다.** 파생 파일이므로 다음 실행 때 덮어쓴다.
  고쳐야 할 내용이 있으면 원본인 `평가결과.md`를 고치고 스크립트를 다시 돌린다.
- 커밋에는 `평가결과.md` · `소재메모.md` · `촬영대기.md` **세 개를 함께 담는다.**
- 스크립트가 출력하는 **경고는 보고에 포함한다:**
  - `👥 중복` / `🔍 유사 제품군` — 같은 제품을 두 번 찍을 위험
  - `소재메모.md 대조` 불일치 — 두 파일의 판정이 어긋난 항목
  - `평가결과.md에 적합 아닌 항목` — 보류·부적합이 잘못 들어간 경우
- 판정 라인 표기를 새로 만들지 말 것. 스크립트는 `**판정: ✅ 적합**` 형식과
  재검증용 `기존: … → 재판정: ✅ 적합(NN점)` 형식만 인식한다. 새 형식을 쓰면 항목이 누락된다.

### 푸시 규칙 (운영자 확정 사항 — 매번 확인 없이 그대로 실행할 것)

- 세션이 작업 브랜치(예: `claude/*`)에서 시작됐더라도, **회차가 끝나면 그 커밋을 `main`에도 반영한다.** 이는 운영자가 명시적으로 지시한 사항이므로 매 회차 다시 묻지 않는다.
- 작업 브랜치가 `main`보다 앞서 있는 정상 상황에서는 **fast-forward로만** 올린다:
  `git push -u origin <작업브랜치>` → `git branch -f main <작업브랜치>` → `git push -u origin main`
- **force push는 하지 않는다.** fast-forward가 불가능하면(= 히스토리가 갈라졌으면) 임의로 덮어쓰지 말고 상황을 보고한다.
- 푸시 후 `git ls-remote origin refs/heads/main`으로 **원격 `main`이 실제로 새 커밋을 가리키는지 확인**하고 결과를 보고한다.
- PR은 운영자가 따로 요청할 때만 만든다.

## 파일 역할

| 파일 | 역할 |
|------|------|
| `소재메모.md` | 운영자 입력 + **중복 검증 판단 원장.** 검증한 제품은 `- [x]` + 날짜 · 판정 |
| `prompts/1_소재검증_프롬프트.md` | 소재 적합성 판단 기준 (운영자가 작성) |
| `prompts/2_대본방향_프롬프트.md` | 대본 방향 도출 기준 (운영자가 작성) |
| `평가결과.md` | **적합 판정된 제품의 정보와 결과만.** 회차마다 아래에 누적 |
| `촬영대기.md` | **적합 목록(제품명·적합여부·검증날짜). 자동 생성 — 직접 수정 금지** |
| `보류부적합_보관.md` | 보류·부적합 상세 보관. 운영 루틴에는 쓰지 않는다 |
| `tools/적합추출.py` | `평가결과.md` → `촬영대기.md` 생성 스크립트 |
| `.claude/commands/validate.md` | 검증 루틴 본체 (수정 불필요) |

---

## 채널 정보  〔여기는 운영자가 채워주세요〕

> 아래 빈칸을 채울수록 검증 정확도가 올라갑니다.

- **채널명**: [여기에 작성]
- **한 줄 콘셉트**: 유명한 제품의 숨겨진 비하인드 스토리, 사람들이 잘 모르지만 좋은 식품, 어린시절 즐기던 추억의 식품 중 사람들이 모르는 스토리
- **주요 타깃 시청자**: 20-30대, 간식 등 먹거리를 좋아하는 사람
- **영상 길이/포맷**: 40초정도 쇼츠
- **채널의 말투/톤**: 구어체, 친근하게 설명함. 존댓말을 사용하지 않음.
- **자주 다루는 소재 유형**: 무조건 쿠팡에서 구매할 수 있는 제품이어야함.
- **절대 다루지 않는 소재**: 한국사람들이 아예 모르는 소재, 쿠팡에 없는 상품, 신선식품이나 식당에서 요리된 음식
